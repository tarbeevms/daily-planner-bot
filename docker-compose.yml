services:
  bot:
    build: .
    image: daily-planner-bot:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=/data/daily_planner.db
    volumes:
      - db-data:/data

volumes:
  db-data:
    driver: local
